# Testing

El backlog exige pruebas de componentes, formularios, validaciones, hooks, servicios, rutas protegidas, roles/permisos, estados de carga/error y flujos críticos de compra.

El package.json solicitado no incorpora un runner de pruebas. Este directorio queda reservado para agregar la suite cuando se seleccione el runner compatible con la versión actual del stack.
